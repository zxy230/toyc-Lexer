int f(int x, int y {
    int z = x + y / 2 % 3;
    if (z < 10 && z > 0 || z <= 20 && z >= 5 || z == 7 || z != 8)
    {
        z = z + 1;
    }
    else
    {
        z = z - ;
    }
    while (z < 100)
    {
        z = z + 1;
        if z == 50) break;
        continue;
    }
    return z;

    void g()
    {
        int i = 0;
        i = f(i, i);
    }

    int main()
    {
        int result = 0;
        result = f(, 2);
        g();
        return result;
    }